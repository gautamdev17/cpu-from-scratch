what my thinking is:
1. split each instruction into stages. smallest units, like tokens.
2. for each of those stages add intermediate regs
3. they all are controlled by control signals. a fsm. in the end.

the fsm sequences everything


mutli cycle is division of an instruction into stages. 
having intermediate regs to store values in each clk cycle. 
but one instruction has to still go through its own stages, in its end, next instruction starts.
 this makes int regs idele. so pipeling make them useful

 so ill go through each inst type. ill add intermediate clocked regs. then think bout a fsm controlling it
look into the pause points. those pause points become states of the fsm.

look through paths of each instr_type. then identify natural pause points.

ok so I'll be writing my ideas on how to divide instructions into cycles.

Ok let's start from first.
R type.
1. Fetch. Store instruction in intermediate reg.
2. Now take that read regs and store that in one intermediate reg.
3. Take those to alu and store alu out in one reg
4. Now take that and write that in reg file

I type except loads and jalr will do the same job. Except for that there will be a mux.
0. Fetch.
1. Rs1 read in reg file stored in intermediate reg. Imm will be combinationally sign extended. 
2. Then add in alu will be stored in a reg
3. Reg file right back one more cycle
That's all 4 cycles

Here's the summary so far:

R-type (4 cycles)
Fetch → latch into IR
Read rs1, rs2 from regfile → latch into A, B registers
ALU using A, B → latch into ALUOut
Write ALUOut to regfile

I-type ALU (4 cycles)
Fetch → latch into IR
Read rs1 from regfile → latch into A. Sign extend imm combinationally
ALU using A, immsext → latch into ALUOut
Write ALUOut to regfile

Ok for store type:
1. Fetch. Store inst in intermediate reg.
2. Read regs and store in intermediate reg. Imm is signed xtended combinationally.
3. Add in alu store that result in intermediate reg.
4. Write in dmem.

S-type Store (4 cycles)
Fetch → latch into IR
Read rs1, rs2 from regfile → latch into A, B. Sign extend imm combinationally
ALU using A, immsext → latch into ALUOut
Write B to dmem at address ALUOut

I'll do I type load now.
1. Fetch. Store inst in intermediate reg.
2. Read regs and store in intermediate reg. Imm is signed xtended combinationally.
3. Alu out is stored in intermediate reg.
4. Dmem is read and written to reg file.
That's all

Since dmem read is slow. I'll split that step. (only for harvard arch implementation)
1. Fetch. Store inst in intermediate reg.
2. Read regs and store in intermediate reg. Imm is signed xtended combinationally.
3. Alu out is stored in intermediate reg.
4. Dmem is read and stored in intermediate reg.
5. Written to reg file.
That's all

Perfect. 5 cycles for load. That's your longest instruction.
Updated full summary:

R-type (4 cycles)
Fetch → IR
Read rs1, rs2 → A, B
ALU using A, B → ALUOut
Write ALUOut to regfile

I-type ALU (4 cycles)
Fetch → IR
Read rs1 → A, sign extend imm
ALU using A, immsext → ALUOut
Write ALUOut to regfile

S-type (4 cycles)
Fetch → IR
Read rs1, rs2 → A, B, sign extend imm
ALU using A, immsext → ALUOut
Write B to dmem at ALUOut

I-type Load (5 cycles)
Fetch → IR
Read rs1 → A, sign extend imm
ALU using A, immsext → ALUOut
Dmem read at ALUOut → MDR
Write MDR to regfile

Pending:
B-type
JAL, JALR
LUI, AUIPC

I think branches are 4 cycles 
1. Fetch. Store inst in intermediate reg.
2. Read regs. Store in intermediate reg.
3. Alu out. Store in intermediate reg.
4. Mux the output(combinational) and write to pc.
That's all.

for jal :
1. fetch
2. sext offest add it to pc and store in intermediate reg
3. writ

see i have an idea at the end of each inst, ill parallely update pc based on control signals thats all.

lui is just 2 cycles. 1. fetch. 2. immsext (comb) write to rd.

auipc is 3 cycles. 1. fetch. 2. pc + immsext(comb) 3. write to rd and pc

jalr 4 cycles
1. fetch
2. read regs (immsext comb)
3. immsext store
4. write to rd and pc
